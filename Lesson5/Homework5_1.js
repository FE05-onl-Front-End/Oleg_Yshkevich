// Превое задание
const data = 3;
const valueToFill = 'a';

const fill = (arraySize, value) => {
  array = [];
  for(let i = 0; i < arraySize; i++){
    array.push(value);
  }
  return array;
}
console.log(fill(data, valueToFill))
